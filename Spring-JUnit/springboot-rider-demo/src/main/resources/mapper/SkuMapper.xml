<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lc.demo.mapper.SkuMapper">

    <select id="loadSkuInfo" resultType="map">
        select * from HW_MASTER_SKU where skuId = #{skuId} and isdelete = '0';
    </select>

</mapper>
